{% extends 'base.html.twig' %}

{% block title %}BlogFoot - Article{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <td><h2 style="font-size: 40px; margin-left: 90px">{{ article.title }}</h2></td>
            </tr>
            <tr>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_article_index') }}"><button class="btn btn-primary">Revenir à la liste des articles</button></a>

    <a href="{{ path('app_admin_article_edit', {'id': article.id}) }}"><button class="btn btn-warning">Modifier l'article</button></a>

    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}
