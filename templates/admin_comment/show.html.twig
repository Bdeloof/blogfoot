{% extends 'base.html.twig' %}

{% block title %}BlogFoot - Commentaires{% endblock %}

{% block body %}
    <h1>Les commentaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Commentaire :</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Poster le :</th>
                <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_comment_index') }}"><button class="btn btn-primary">Revenir à la liste des commentaires</button></a>

    <a href="{{ path('app_admin_comment_edit', {'id': comment.id}) }}"><button class="btn btn-warning">Modifier le commentaire</button></a>

    {{ include('admin_comment/_delete_form.html.twig') }}
{% endblock %}
